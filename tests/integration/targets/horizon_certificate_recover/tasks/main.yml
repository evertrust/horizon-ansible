- name: Recover a certificate by its pem file
  evertrust.horizon.horizon_recover:

    endpoint: "{{ endpoint }}"
    ca_bundle: "{{ ca_bundle }}"
    x_api_id: "{{ x_api_id }}"
    x_api_key: "{{ x_api_key }}"

    certificate_pem: 
      src: "/tmp/PEM.pem"

    password: "Pass-W0rd"

