- name: Recover a certificate by its pem file
  no_log: true
  evertrust.horizon.horizon_recover:

    endpoint: "{{ endpoint }}"
    x_api_id: "{{ x_api_id }}"
    x_api_key: "{{ x_api_key }}"

    certificate_pem:
      src: "/tmp/PEM.pem"

    password: "Pass-W0rd"
