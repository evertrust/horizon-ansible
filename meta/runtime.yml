---
requires_ansible: '>=2.9.10'
action_groups:
  horizon:
    - horizon_enroll
    - horizon_recover
    - horizon_renew
    - horizon_revoke
    - horizon_update
    - horizon_feed
    - horion_inventory
    - horizon_lookup
    - horizon
