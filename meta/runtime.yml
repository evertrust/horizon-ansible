---
requires_ansible: '>=2.19.11'
action_groups:
  horizon:
    - horizon_enroll
    - horizon_recover
    - horizon_renew
    - horizon_revoke
    - horizon_update
    - horizon_feed
    - horizon_inventory
    - horizon_lookup
    - horizon_template
    - horizon_request_enroll
    - horizon_get_certificate
    - horizon
